angular.module('how').controller('HowPrimaryController',
  [
    '$scope', '$location', '$log',
    function(
      $scope, $location, $log
    ) {
      console.log('HowPrimaryController initialized');
    }
  ]
)