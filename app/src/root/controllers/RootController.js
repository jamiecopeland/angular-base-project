angular.module('root').controller('RootController',
  // [
  //   '$scope', '$location', '$log',
    function(
      $scope, $location, $log
    ) {
      // adsf.something()

      $log.log('RootController initialized');
    }
  // ]
)