angular.module('root').controller('RootController',
  [
    '$scope', '$location', '$log',
    function(
      $scope, $location, $log
    ) {
      
      console.log('RootController initialized');

    }
  ]
)